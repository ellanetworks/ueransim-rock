name: ubuntu-router
base: ubuntu:22.04
version: '0.1'
summary: A Linux router.
description: |
    This container image is used to operate Linux iptables routers.
license: Apache-2.0
platforms:
  amd64:

parts:
  iptables:
    plugin: nil
    stage-packages:
      - iptables
